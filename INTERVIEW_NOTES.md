# 面试答辩要点

- 为什么不用 Agent SDK：题目要求关键逻辑自行实现；显式循环更容易审计、测试和解释。
- 为什么工具少：六个工具覆盖读、搜、写、替换、执行，减少模型选择空间，也缩小风险面。
- 为什么先读后写：系统提示要求 inspect-before-edit；`replace_text` 还要求精确匹配次数，避免静默破坏用户代码。
- 如何防越界：`Path.resolve()` 后强制 `relative_to(workspace)`；所有工具共享同一边界。
- 如何处理错误：工具返回结构化 `{ok:false,error}`，模型可基于错误修正；同一调用连续两次后熔断，避免死循环。
- 如何控制上下文：工具输出截断；压缩时保留 system/user 前缀和完整 assistant/tool 交互块。
- 如何保证可追溯：每次模型回复、工具结果、停止原因写入 JSONL；凭据模式在落盘前脱敏。
- 为什么测试不依赖 API：脚本 provider 模拟真实 tool-call 序列，端到端验证循环、审批、越界与高危命令阻断。

